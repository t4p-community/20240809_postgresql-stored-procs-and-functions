-- This SQL query calls the 'insert_category' procedure with a specific category name.
-- The 'insert_category' procedure inserts a new record into the 'category' table with the provided name.

-- Call the procedure with the category name 'New Category'.
CALL insert_category('New Category');