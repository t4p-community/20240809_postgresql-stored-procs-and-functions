-- This SQL query calls the 'delete_category_by_name' procedure with a specific category name.
-- The 'delete_category_by_name' procedure deletes the category with the specified name from the 'category' table.

-- Call the procedure with the category name 'New Category'.
CALL delete_category_by_name('New Category');